/**
 news入口
 **/
layui.define(['layer', 'flow','element','carousel','util'], function(exports){
    var layer = layui.layer,
        element = layui.element ,
        flow = layui.flow,
        carousel = layui.carousel,
        util = layui.util;
    exports('selfspace', {}); //注意，这里是模块输出的核心，模块名必须和use时的模块名一致
});
